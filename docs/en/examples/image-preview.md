# Image Preview

<DemoPreview src="/components/image-preview.html" title="Image Preview" :height="430" />

Click the image and use the mouse wheel to zoom. The default preset is centered and scrollbar-free; this demo enables the optional toolbar with `showToolbar: true`.

See the [Image Preview API](/en/api/image-preview) for sizing, scale limits, overflow, and cleanup options.
